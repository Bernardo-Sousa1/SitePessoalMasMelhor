@model IEnumerable<SitePessoalMasMelhor.Models.SobreMim>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sobre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sobre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SobreMimId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SobreMimId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SobreMimId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
